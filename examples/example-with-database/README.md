# Database Example: Terraform Webapp Infrastructure

This example shows how to deploy a public web container with both RDS and DynamoDB databases on AWS using the module.

## Usage

```hcl
terraform init
terraform plan
terraform apply
```